# Instruções de Uso

Antes de executar o script, instale, primeiramente, as dependências:

```bash
pip install -r requirements.txt
```

Agora basta executar o código:

```bash
python scraper.py
```
